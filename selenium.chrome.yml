services:
  webserver:
    environment:
      MOODLE_DOCKER_BROWSER: chrome
  selenium:
    image: "${MOODLE_DOCKER_SELENIUM_REPOSITORY}/standalone-chromium:${MOODLE_DOCKER_BROWSER_TAG}"
    volumes:
        - /dev/shm:/dev/shm
